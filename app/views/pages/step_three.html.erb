<div class="third">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-6">
        <h3>Félicitations! La première partie de ton dossier de location est déjà prête !</h3>
        <h5>Une fois ton dossier complété, tu pourras l'envoyer aux propriétaires de logement correspondant à tes critères.</h5>
        <h5>Cependant, il reste quelques étapes à compléter pour avoir un dossier digne de ce nom !</h5>
        <%= link_to "Continuer !", step_path('3-1'), class: "btn custom-btn" %>
      </div>
      <div class="col-xs-12 col-md-6">
        <div class="land">
          <%= image_tag "growing.png", style:" <width:400px;", class: "landing_img" %>
        </div>
      </div>
    </div>
  </div>
  <div class="document" class="container">
    <div>
      <%= image_tag "logo_doc.png", class: "dossier_logo" %>
    </div>
    <div class="dossier_candidates">
      <% x = @candidates.size %>
      <% @candidates.each do |candidate| %>
        <% now = Time.now.utc.to_date %>
        <% bd = now.year - candidate.birthdate.year - (candidate.birthdate.to_date.change(:year => now.year) > now ? 1 : 0) %>
        <h4><i class="fa fa-user"></i> <%= "#{candidate.firstname} #{candidate.lastname}" %> - <%= bd %> ans</h4>
        <p><i class="fa fa-envelope-o"></i> hello@world.com</p>
        <p><i class="fa fa-phone"></i> (01) 42 42 42 42</p>

        <% @bio_name = "#{candidate.firstname} #{candidate.lastname}" %>
      <% end %>
    </div>
    <ul class="list-inline">
      <li><%= image_tag "default-user.png", class: "img-circle biopic"%></li>
    </ul>
    <div class="separator">
      <h4>Biography</h4>
    </div>
    <div>
      <h4 class="revenue"> <%= "Loyer Maximum : #{@candidates.first.dossier_max_rent}€" %></h4>
    </div>
    <div class="separator">
      <h4>Employement and Income</h4>
    </div>
    <div>
      <h4 class="revenue">Past Rentals</h4>
    </div>
    <div class="separator">
      <h4>References</h4>
    </div>
  </div>
</div>

